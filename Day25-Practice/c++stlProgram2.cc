#include<bits/stdc++.h>
#include<vector>
using namespace std;
void show(vector<int> v){ for(int value:v)cout<<value<<" ";}

int main(){
    // vector<int> vet{2,45,3,4,5};
    // vector<int> v(vet.begin(),vet.end());
    // sort(v.begin(),v.end(),greater<>());
    // show(v);

    int arr[]={0};
    for(int i=0;i<5;i++)cout<<arr[i]<<" ";
    
    return 0;
}